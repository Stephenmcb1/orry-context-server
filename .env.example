# Copy to .env.local for local dev (.env.local is gitignored — never commit secrets).
# In production these are set in the Vercel dashboard, not in the repo.

SUPABASE_URL=https://uiogkpytfnjewekeowgr.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
MCP_ENABLE_WRITE=
